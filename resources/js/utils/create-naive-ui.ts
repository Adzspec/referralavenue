// utils/create-naive-ui.ts
import {
    create,
    NButton,
    NDataTable,
    NMessageProvider,
    NDialogProvider,
    NNotificationProvider,
    NPagination
} from 'naive-ui';

export default create({
    components: [
        NButton,
        NDataTable,
        NMessageProvider,
        NDialogProvider,
        NNotificationProvider,
        NPagination,
    ],
});
